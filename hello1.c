#include <stdio.h>

int main() {
    
    enum gender {male, female};
    enum gender myGender;
    myGender = male;

    if (myGender == male) {
        printf("Gender: Male\n");
    } else {
        printf("Gender: Female\n");
    }
    
    printf("%c", a); // Output the integer value of

    return 0;
}
